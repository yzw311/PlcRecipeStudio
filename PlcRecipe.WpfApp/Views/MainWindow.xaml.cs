using System.Windows;

namespace PlcRecipe.WpfApp;

/// <summary>主窗口。code-behind 仅 InitializeComponent，全部逻辑在 MainViewModel。</summary>
public partial class MainWindow : Window
{
    public MainWindow()
    {
        InitializeComponent();
    }
}
